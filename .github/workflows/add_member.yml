# This workflow will add a member to the organization when an issue is labeled with "approved"

name: Invite member to organization

on:
  issues:
    types: [labeled]

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          script: |
           console.log(issue.payload)
